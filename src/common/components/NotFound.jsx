import { Typography } from "antd";
import React from "react";

const NotFound = () => {
  const { Title } = Typography;
  return <Title level={1}> PAGE NOT FOUND </Title>;
};

export default NotFound;
